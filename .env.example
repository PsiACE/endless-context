# Endless Context minimal configuration for Bub + SeekDB (OceanBase-compatible)
# Copy this file to .env and fill in your provider credentials.

# Database (SeekDB via OceanBase-compatible protocol)
OCEANBASE_HOST=127.0.0.1
OCEANBASE_PORT=2881
OCEANBASE_USER=root
OCEANBASE_PASSWORD=
OCEANBASE_DATABASE=bub
BUB_TAPE_TABLE=bub_tape_entries

# Bub runtime
BUB_MODEL=openai:qwen-plus
BUB_MAX_TOKENS=2048
# Optional explicit tape namespace for session keys
# BUB_TAPE_NAME=endless-context

# LLM credentials (Bub resolves from BUB_API_KEY / LLM_API_KEY / OPENROUTER_API_KEY)
LLM_API_KEY=your_api_key_here
LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
