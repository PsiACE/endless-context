# Endless Context minimal configuration for Republic + SeekDB (OceanBase-compatible)
# Copy this file to .env and fill in your provider credentials.

# Database (SeekDB via OceanBase-compatible protocol)
DATABASE_PROVIDER=oceanbase
OCEANBASE_HOST=127.0.0.1
OCEANBASE_PORT=2881
OCEANBASE_USER=root
OCEANBASE_PASSWORD=
OCEANBASE_DATABASE=republic
REPUBLIC_TAPE_TABLE=republic_tape_entries

# LLM (Qwen uses OpenAI-compatible DashScope endpoint)
LLM_PROVIDER=openai
LLM_API_KEY=your_api_key_here
LLM_MODEL=qwen-plus
LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1

# Optional Republic overrides
# REPUBLIC_MODEL=openai:qwen-plus
# REPUBLIC_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# REPUBLIC_VERBOSE=0
